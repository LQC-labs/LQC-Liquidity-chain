(function(){"use strict";
const DEPLOYER="0x7cf23bB16Ed0E1eaF58CD31c9F5a643be438C6aB",OWNER="0x5235e26EE4D511aE8ba1FB1cff2619Fc1D90C02A",EXPECTED_CHAIN=97,TIMEOUT_MS=20000;
const FEEDS=[{id:"tLQC Primary",answer:100000000n},{id:"tLQC Secondary",answer:99000000n},{id:"WBNB Primary",answer:60000000000n},{id:"WBNB Secondary",answer:59400000000n}],$=id=>document.getElementById(id),hex=n=>"0x"+BigInt(n).toString(16),host=value=>{try{return new URL(value).hostname}catch{return"invalid-url"}};
function status(message,type="info"){$("status").textContent=message;$("status").dataset.type=type}function consumeFragment(){const match=location.hash.match(/^#rpc=(.+)$/);if(!match)return;try{const value=decodeURIComponent(match[1]);if(!value.startsWith("https://"))throw Error();$("privateRpc").value=value;status("QuickNode 주소가 안전하게 입력됐습니다. 검사 버튼을 누르세요.")}catch{status("자동 입력 주소가 잘못되었습니다.","error")}finally{history.replaceState(null,"",location.pathname+location.search)}}
async function rpc(url,method,params=[]){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),TIMEOUT_MS);try{const response=await fetch(url,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({jsonrpc:"2.0",id:1,method,params}),signal:controller.signal,cache:"no-store",credentials:"omit",referrerPolicy:"no-referrer"});if(!response.ok)throw Error(`HTTP ${response.status}`);const body=await response.json();if(body.error)throw Error(body.error.message||"RPC error");if(body.result===undefined)throw Error("RPC result missing");return body.result}finally{clearTimeout(timer)}}
async function initCodes(){const response=await fetch("./lending-stage0-price-feed-artifact.json",{cache:"no-store",credentials:"omit"});if(!response.ok)throw Error("Feed artifact를 읽지 못했습니다.");const artifact=await response.json(),factory=new ethers.ContractFactory(artifact.abi,artifact.bytecode);return Promise.all(FEEDS.map(async feed=>(await factory.getDeployTransaction(OWNER,8,feed.answer)).data))}
async function head(url){const [chain,number]=await Promise.all([rpc(url,"eth_chainId"),rpc(url,"eth_blockNumber")]);if(Number(BigInt(chain))!==EXPECTED_CHAIN)throw Error(`${host(url)}: chain 97이 아닙니다.`);return{url,label:host(url),head:Number(BigInt(number))}}
async function observe(provider,block,codes){const tag=hex(block),blockData=await rpc(provider.url,"eth_getBlockByNumber",[tag,false]);if(!blockData?.hash||blockData.baseFeePerGas==null)throw Error(`${provider.label}: canonical block/base fee가 없습니다.`);const [nonceHex,balanceHex,gasPriceHex]=await Promise.all([rpc(provider.url,"eth_getTransactionCount",[DEPLOYER,tag]),rpc(provider.url,"eth_getBalance",[DEPLOYER,tag]),rpc(provider.url,"eth_gasPrice")]),nonce=Number(BigInt(nonceHex)),addresses=FEEDS.map((_,i)=>ethers.getCreateAddress({from:DEPLOYER,nonce:nonce+i}));const [runtime,gas]=await Promise.all([Promise.all(addresses.map(address=>rpc(provider.url,"eth_getCode",[address,tag]))),Promise.all(codes.map(data=>rpc(provider.url,"eth_estimateGas",[{from:DEPLOYER,data,value:"0x0"}])))]);if(runtime.some(value=>value!=="0x"))throw Error(`${provider.label}: 예상주소에 이미 코드가 있습니다.`);const baseFee=BigInt(blockData.baseFeePerGas),gasPrice=BigInt(gasPriceHex);if(gasPrice<=0n)throw Error(`${provider.label}: 유효한 gas price를 읽지 못했습니다.`);return{...provider,blockHash:blockData.hash.toLowerCase(),baseFee,gasPrice,nonce,balance:BigInt(balanceHex),addresses,gas:gas.map(value=>BigInt(value))}}
function format(value,decimals=18){const unit=10n**BigInt(decimals),whole=value/unit,fraction=(value%unit).toString().padStart(decimals,"0").replace(/0+$/,"" );return fraction?`${whole}.${fraction}`:whole.toString()}
$("verify").onclick=async()=>{const button=$("verify"),first=$("publicRpc").value.trim(),second=$("privateRpc").value.trim();try{button.disabled=true;if(!window.ethers)throw Error("검증 라이브러리를 불러오지 못했습니다.");if(!first.startsWith("https://")||!second.startsWith("https://")||host(first)===host(second))throw Error("서로 독립된 HTTPS RPC 두 개가 필요합니다.");status("Oracle 4개 주소와 가스를 두 RPC에서 검사 중입니다…");const [codes,providers]=await Promise.all([initCodes(),Promise.all([head(first),head(second)])]),block=Math.min(...providers.map(value=>value.head)),values=await Promise.all(providers.map(value=>observe(value,block,codes))),a=values[0],b=values[1];if(a.blockHash!==b.blockHash||a.baseFee!==b.baseFee||a.nonce!==b.nonce||a.balance!==b.balance||a.addresses.join().toLowerCase()!==b.addresses.join().toLowerCase())throw Error("두 RPC의 canonical 상태가 다릅니다.");for(let i=0;i<4;i++){const high=a.gas[i]>b.gas[i]?a.gas[i]:b.gas[i],low=a.gas[i]>b.gas[i]?b.gas[i]:a.gas[i];if((high-low)*100n>high*5n)throw Error(`${FEEDS[i].id}: RPC 가스 추정 편차가 5%를 초과했습니다.`)}const gas=a.gas.map((value,i)=>value>b.gas[i]?value:b.gas[i]),total=gas.reduce((sum,value)=>sum+value,0n),approvedLimits=gas.map(value=>(value*120n+99n)/100n),totalApproved=approvedLimits.reduce((sum,value)=>sum+value,0n),gasPrice=a.gasPrice>b.gasPrice?a.gasPrice:b.gasPrice,required=totalApproved*gasPrice;if(a.balance<required)throw Error("Oracle 4개 배포용 tBNB 잔액이 부족합니다.");$("state").textContent=`block ${block}\nblock hash ${a.blockHash}\nnonce ${a.nonce}\nbalance ${format(a.balance)} tBNB\nbase fee ${format(a.baseFee,9)} Gwei\n보수적 gas price ${format(gasPrice,9)} Gwei`;$("deployments").textContent=FEEDS.map((feed,i)=>`${i+1}. ${feed.id}\n${a.addresses[i]}\ngas ${gas[i]} / limit ${approvedLimits[i]}`).join("\n\n");$("budget").textContent=`총 보수적 gas ${total}\n총 승인 gas limit ${totalApproved}\n정확한 최대예산 ${format(required)} tBNB`;status("두 RPC 사전검증 통과. 네 예상주소가 비어 있고 가스 편차·잔액 조건이 충족됐습니다. 거래는 발생하지 않았습니다.","ok")}catch(error){status(error.name==="AbortError"?"RPC 응답 시간이 초과됐습니다.":(error.message||String(error)),"error")}finally{button.disabled=false}};
window.addEventListener("pagehide",()=>{$("privateRpc").value=""});consumeFragment();
})();
