(function(){
  "use strict";
  const HASH=/^0x[0-9a-fA-F]{64}$/,MAX_AGE_MS=15*60*1000,MAX_FUTURE_MS=5*60*1000,MAX_LATEST_AGE_MS=2*60*1000,SIGNER="0x7cf23bb16ed0e1eaf58cd31c9f5a643be438c6ab",FLOW="0x7d3375921fc0a5becb9b20cbfdcf03440befc5113d864e8522b8465ca68b8014",ADAPTER="0x14db750acf95b469aba3e74032e6db61087ef4cd",TLQC="0x84a30a66cfcbb15453c83204b7e6ec436a0718fc",WBNB="0xae13d989dac2f0debff460ac112a837c89baa7cd",AMOUNT=10n*10n**18n,PROOF_RUNTIME_HASH="0x5b529479796e79c39d4959695425be49edde0cc9eecce4f49e6605e38ba37f3a",GATEWAY_RUNTIME_HASH="0x65c43d36bf8c742d97860797ee71c587ec3e03803717227c3d0d90797f5a2f15";
  const REQUEST="tuple(uint256 chainId,address tokenIn,address tokenOut,uint256 amountIn,address recipient,uint16 slippageBps,uint64 validUntil)",RESULT="tuple(bytes32 dexId,address adapter,uint256 quoteBlock,uint256 grossAmountOut,uint256 gasCostInTokenOut,uint256 protocolFeeInTokenOut,uint256 netAmountOut,uint256 minimumAmountOut,uint32 priority,bytes32 routeHash)",GATEWAY_ABI=["function executeBestCandidate("+REQUEST+","+RESULT+"[],bytes[],uint256) returns(bytes32,uint256)","event ProofBoundSwapExecuted(bytes32 indexed proofHash,address indexed sender,bytes32 indexed dexId,uint256 amountIn,uint256 amountOut)"],PROOF_ABI=["function bestCandidateProofHash("+REQUEST+","+RESULT+"[],uint256) pure returns(bytes32)"];
  const $=id=>document.getElementById(id),gatewayInterface=new ethers.Interface(GATEWAY_ABI);let receipt=null,verifiedEntries=[];
  function status(message,type="info"){$("status").textContent=message;$("status").dataset.type=type;}
  function exact(value,keys){const a=Object.keys(value).sort(),b=[...keys].sort();if(a.length!==b.length||a.some((key,index)=>key!==b[index]))throw new Error("허용되지 않은 필드가 있거나 필수 필드가 없습니다.");}
  function integer(value,name){if(!Number.isSafeInteger(value)||value<0)throw new Error(name+" 값이 올바르지 않습니다.");}
  function hash(value,name){if(typeof value!=="string"||!HASH.test(value))throw new Error(name+" 해시가 올바르지 않습니다.");}
  function verifyArchive(entry){exact(entry,["format","archivedAt","transactionHash","readiness","execution","executionFinal","archiveHash"]);if(entry.format!=="LQC_PROOF_GATEWAY_EXECUTION_ARCHIVE_V1")throw new Error("지원하지 않는 Archive 형식입니다.");integer(entry.archivedAt,"보관 시각");hash(entry.transactionHash,"거래");hash(entry.archiveHash,"Archive");const {archiveHash,...core}=entry,computed=ethers.keccak256(ethers.toUtf8Bytes(JSON.stringify(core)));if(computed.toLowerCase()!==archiveHash.toLowerCase())throw new Error("Archive Hash가 일치하지 않습니다.");if(entry.execution?.transactionHash?.toLowerCase()!==entry.transactionHash.toLowerCase()||entry.executionFinal?.transactionHash?.toLowerCase()!==entry.transactionHash.toLowerCase()||entry.readiness?.proofHash?.toLowerCase()!==entry.execution?.proofHash?.toLowerCase()||entry.execution?.proofHash?.toLowerCase()!==entry.executionFinal?.proofHash?.toLowerCase()||entry.readiness?.proofGateway?.toLowerCase()!==entry.execution?.gateway?.toLowerCase()||entry.execution?.gateway?.toLowerCase()!==entry.executionFinal?.gateway?.toLowerCase())throw new Error("Archive 거래·Proof·Gateway 연결이 일치하지 않습니다.");hash(entry.execution.proofHash,"Proof");if(entry.executionFinal.amountIn!==AMOUNT.toString()||entry.execution.actualAmountOut!==entry.executionFinal.amountOut||entry.executionFinal.residualAllowance!=="0")throw new Error("Archive 입출력 또는 승인 잔액이 검토 범위와 다릅니다.");return entry;}
  function verify(){try{if(!window.ethers)throw new Error("검증 라이브러리를 불러오지 못했습니다.");const history=JSON.parse($("history").value),claim=JSON.parse($("receipt").value);exact(history,["format","chainId","count","entries"]);if(history.format!=="LQC_PROOF_GATEWAY_EXECUTION_HISTORY_V1"||history.chainId!==97||!Array.isArray(history.entries)||history.entries.length>20||history.count!==history.entries.length)throw new Error("거래 이력 백업 형식이 올바르지 않습니다.");const entries=history.entries.map(verifyArchive);exact(claim,["format","chainId","count","archiveRoot","latestBlockNumber","latestBlockHash","verifiedAt","verificationHash"]);if(claim.format!=="LQC_PROOF_GATEWAY_HISTORY_VERIFICATION_V1"||claim.chainId!==97||claim.count!==entries.length)throw new Error("감사 영수증 형식·체인·건수가 다릅니다.");integer(claim.latestBlockNumber,"최신 블록");integer(claim.verifiedAt,"검증 시각");hash(claim.archiveRoot,"Archive Root");hash(claim.latestBlockHash,"최신 블록");hash(claim.verificationHash,"Verification");const root=ethers.keccak256(ethers.concat(entries.map(entry=>entry.archiveHash)));if(root.toLowerCase()!==claim.archiveRoot.toLowerCase())throw new Error("Archive Root가 거래 이력과 일치하지 않습니다.");const {verificationHash,...core}=claim,computed=ethers.keccak256(ethers.toUtf8Bytes(JSON.stringify(core)));if(computed.toLowerCase()!==verificationHash.toLowerCase())throw new Error("History Verification Hash가 일치하지 않습니다.");const age=Date.now()-claim.verifiedAt;if(age < -MAX_FUTURE_MS)throw new Error("검증 시각이 현재보다 5분 이상 미래입니다.");receipt=claim;verifiedEntries=entries;$("result").textContent="통과 · "+entries.length+"건 · "+(age<=MAX_AGE_MS?"최신":"15분 경과");$("root").textContent=root;$("verification").textContent=verificationHash;$("live").textContent="확인 전";status(age<=MAX_AGE_MS?"독립 무결성 검증 통과. 이제 모든 거래를 온체인에서 확인하세요.":"무결성은 통과했지만 오래된 영수증입니다. 새 온체인 이력 검증 영수증을 만드세요.",age<=MAX_AGE_MS?"ok":"warning");}catch(error){receipt=null;verifiedEntries=[];$("result").textContent="검증 실패";$("root").textContent="—";$("verification").textContent="—";status(error.message||String(error),"error");}}
  async function liveVerify(){try{if(!receipt||!verifiedEntries.length)throw new Error("먼저 두 JSON의 독립 검증을 통과하세요.");if(!window.ethereum)throw new Error("TokenPocket DApp 브라우저에서 열어주세요.");const provider=new ethers.BrowserProvider(window.ethereum);if((await provider.getNetwork()).chainId!==97n)throw new Error("BSC Testnet(chain 97)이 아닙니다.");const [block,latest]=await Promise.all([provider.getBlock(receipt.latestBlockNumber),provider.getBlock("latest")]);if(!block||block.hash.toLowerCase()!==receipt.latestBlockHash.toLowerCase())throw new Error("영수증 블록 해시가 현재 정식 체인과 다릅니다.");const latestAge=Date.now()-latest.timestamp*1000;if(!latest?.hash||latestAge>MAX_LATEST_AGE_MS||latestAge < -60*1000)throw new Error("RPC 최신 블록 시각이 현재와 맞지 않습니다.");const receiptConfirmations=latest.number-block.number+1;if(receiptConfirmations<12)throw new Error("영수증 블록 확인이 12개보다 적습니다.");for(const entry of verifiedEntries){const final=entry.executionFinal,gateway=entry.execution.gateway,[chainReceipt,transaction,code]=await Promise.all([provider.getTransactionReceipt(entry.transactionHash),provider.getTransaction(entry.transactionHash),provider.getCode(gateway)]);if(!chainReceipt||!transaction||chainReceipt.status!==1||chainReceipt.blockHash.toLowerCase()!==final.blockHash.toLowerCase()||chainReceipt.blockNumber!==final.blockNumber)throw new Error("과거 거래 영수증·블록이 이력과 다릅니다.");if(transaction.hash.toLowerCase()!==entry.transactionHash.toLowerCase()||transaction.from.toLowerCase()!==SIGNER||transaction.to?.toLowerCase()!==gateway.toLowerCase()||transaction.value!==0n)throw new Error("과거 거래 원문의 해시·보낸이·Gateway·전송값이 다릅니다.");if(code==="0x"||ethers.keccak256(code)!==GATEWAY_RUNTIME_HASH)throw new Error("Gateway 현재 코드가 검토본과 다릅니다.");const proofCode=await provider.getCode(entry.readiness.proofVerifier);if(proofCode==="0x"||ethers.keccak256(proofCode)!==PROOF_RUNTIME_HASH)throw new Error("Proof 현재 코드가 검토본과 다릅니다.");const parsed=gatewayInterface.parseTransaction({data:transaction.data,value:transaction.value});if(!parsed||parsed.name!=="executeBestCandidate")throw new Error("과거 거래 calldata가 Gateway 실행 함수가 아닙니다.");const requestData=parsed.args[0],candidates=parsed.args[1],routes=parsed.args[2],selectedIndex=parsed.args[3];if(requestData.chainId!==97n||requestData.tokenIn.toLowerCase()!==TLQC||requestData.tokenOut.toLowerCase()!==WBNB||requestData.amountIn!==AMOUNT||requestData.recipient.toLowerCase()!==SIGNER||selectedIndex>=BigInt(candidates.length)||routes.length!==candidates.length)throw new Error("과거 거래 calldata의 체인·토큰·수량·수령자·후보 범위가 다릅니다.");const selected=candidates[Number(selectedIndex)];if(selected.dexId.toLowerCase()!==FLOW||selected.adapter.toLowerCase()!==ADAPTER)throw new Error("과거 거래의 선택 경로 또는 Adapter가 다릅니다.");const proofContract=new ethers.Contract(entry.readiness.proofVerifier,PROOF_ABI,provider),computedProof=await proofContract.bestCandidateProofHash(requestData,candidates,selectedIndex);if(computedProof.toLowerCase()!==entry.execution.proofHash.toLowerCase())throw new Error("과거 거래 calldata의 Proof Hash가 이력과 다릅니다.");const txConfirmations=latest.number-chainReceipt.blockNumber+1;if(txConfirmations<12)throw new Error("과거 거래 확인이 12개보다 적습니다.");const topic=gatewayInterface.getEvent("ProofBoundSwapExecuted").topicHash,eventLog=chainReceipt.logs.find(log=>log.address.toLowerCase()===gateway.toLowerCase()&&log.topics[0]===topic);if(!eventLog)throw new Error("과거 거래의 ProofBoundSwapExecuted 이벤트가 없습니다.");const event=gatewayInterface.parseLog(eventLog);if(event.args.proofHash.toLowerCase()!==entry.execution.proofHash.toLowerCase()||event.args.sender.toLowerCase()!==SIGNER||event.args.dexId.toLowerCase()!==FLOW||event.args.amountIn!==AMOUNT||event.args.amountOut.toString()!==final.amountOut)throw new Error("과거 거래 이벤트의 Proof·Signer·경로·입출력이 다릅니다.");const contract=new ethers.Contract(gateway,["function consumedProof(bytes32) view returns(bool)"],provider);if(!await contract.consumedProof(entry.execution.proofHash))throw new Error("과거 거래 Proof가 현재 소비 상태가 아닙니다.");}$("live").textContent="통과 · "+verifiedEntries.length+"건 · 기준 블록 "+block.number+" · 모든 영수증·이벤트·코드·Proof 일치";status("독립 영수증과 모든 과거 거래가 현재 BSC Testnet에서 일치합니다. 계정 요청·서명·거래는 발생하지 않았습니다.","ok");}catch(error){$("live").textContent="실패 · "+(error.message||String(error));status(error.message||String(error),"error");}}
  async function paste(id){try{$(id).value=await navigator.clipboard.readText();status("클립보드 JSON을 불러왔습니다.");}catch(error){status("클립보드를 읽지 못했습니다.","error");}}
  $("pasteHistory").addEventListener("click",()=>paste("history"));$("pasteReceipt").addEventListener("click",()=>paste("receipt"));$("verify").addEventListener("click",verify);$("liveVerify").addEventListener("click",liveVerify);
})();
