import fs from "node:fs";
import path from "node:path";
import { pathToFileURL } from "node:url";
import { canonicalDigest, sha256 } from "./build-intent-reproducibility-seal.mjs";
import { buildIntentStage4AuditHandoff, renderIntentStage4AuditHandoffMarkdown } from "./build-intent-stage4-audit-handoff.mjs";
const REQUIRED=["HANDOFF.md","audit-gate.json","audit-verification.json","completion-manifest.json","handoff-manifest.json"],EVIDENCE=REQUIRED.filter(name=>name!=="handoff-manifest.json");
export function verifyIntentStage4AuditHandoff(directoryPath){const directory=path.resolve(directoryPath),files=fs.readdirSync(directory).sort();if(canonicalDigest(files)!==canonicalDigest(REQUIRED))throw new Error("Stage-4 audit handoff file set mismatch");const text=name=>fs.readFileSync(path.join(directory,name),"utf8"),json=name=>JSON.parse(text(name)),inputs={completion:json("completion-manifest.json"),auditRecord:json("audit-gate.json"),auditVerification:json("audit-verification.json")},rebuilt=buildIntentStage4AuditHandoff(inputs),manifest=json("handoff-manifest.json"),manifestBody=Object.fromEntries(Object.entries(manifest).filter(([key])=>key!=="bundleDigest"));if(canonicalDigest(manifestBody)!==manifest.bundleDigest)throw new Error("Stage-4 audit handoff bundle digest mismatch");const expectedFiles=Object.fromEntries(EVIDENCE.map(name=>[name,sha256(text(name))]));if(canonicalDigest(manifest.files)!==canonicalDigest(expectedFiles))throw new Error("Stage-4 audit handoff file digest mismatch");if(canonicalDigest(manifestBody)!==canonicalDigest({...rebuilt,files:expectedFiles}))throw new Error("Stage-4 audit handoff manifest mismatch");if(text("HANDOFF.md")!==renderIntentStage4AuditHandoffMarkdown(rebuilt))throw new Error("Stage-4 audit handoff summary mismatch");const body={schemaVersion:1,recordType:"LQC_INTENT_STAGE4_AUDIT_HANDOFF_VERIFICATION",status:"VERIFIED_STAGE4_AUDIT_HANDOFF",network:rebuilt.network,sourceRevision:rebuilt.sourceRevision,intentHash:rebuilt.intentHash,quoteHash:rebuilt.quoteHash,executionHash:rebuilt.executionHash,handoffDigest:rebuilt.handoffDigest,bundleDigest:manifest.bundleDigest,evidenceFileDigests:expectedFiles,transactionOccurred:false,safety:"Offline Stage-4 handoff verification only. No RPC, wallet, key, signature, settlement, token movement, deployment, or transaction."};return{...body,handoffVerificationDigest:canonicalDigest(body)};}
async function main(){const[directory,outputFile]=process.argv.slice(2);if(!directory)throw new Error("Usage: node verify-intent-stage4-audit-handoff.mjs <handoff-dir> [output.json]");const result=verifyIntentStage4AuditHandoff(directory);if(outputFile)fs.writeFileSync(path.resolve(outputFile),`${JSON.stringify(result,null,2)}\n`,{flag:"wx"});else console.log(JSON.stringify(result,null,2));}
if(process.argv[1]&&import.meta.url===pathToFileURL(process.argv[1]).href)await main();
