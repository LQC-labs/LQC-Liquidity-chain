import fs from "node:fs";import { ethers } from "ethers";import { buildLendingStage4Manifest } from "./prepare-lending-stage4-manifest.mjs";import { canonicalDigest } from "./build-intent-reproducibility-seal.mjs";
export async function buildStage4BrowserGasPacket({reviewInput,liveObservation}){const m=await buildLendingStage4Manifest({reviewInput}),a=m.orderedActions[2];if(liveObservation.deployerNonce!=="110"||liveObservation.predictedLiquidationEngine.toLowerCase()!==ethers.getCreateAddress({from:liveObservation.deployer,nonce:110}).toLowerCase())throw Error("Stage4 observation drift");const body={schemaVersion:1,recordType:"LQC_LENDING_STAGE4_BROWSER_GAS_PACKET",status:"READ_ONLY_GAS_ESTIMATE_ONLY",network:m.network,manifestDigest:m.manifestDigest,deployer:ethers.getAddress(liveObservation.deployer),deployerNonce:liveObservation.deployerNonce,predictedLiquidationEngine:liveObservation.predictedLiquidationEngine,engineInitCode:a.data,engineInitCodeDigest:a.initCodeDigest,transactionOccurred:false,safety:"Compile-backed initCode transport for browser eth_estimateGas only. No signing or broadcast."};return{...body,packetDigest:canonicalDigest(body)}}

