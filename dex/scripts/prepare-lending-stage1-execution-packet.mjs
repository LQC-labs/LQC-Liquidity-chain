import fs from "node:fs";
import path from "node:path";
import { pathToFileURL } from "node:url";
import { canonicalDigest } from "./build-intent-reproducibility-seal.mjs";
import { buildLendingStage1DeploymentReview } from "./build-lending-stage1-deployment-review.mjs";

export function prepareLendingStage1ExecutionPacket({manifest,config,preflight,review,approvedReviewDigest}){
  const expected=buildLendingStage1DeploymentReview({manifest,config,preflight}),{reviewDigest,...reviewBody}=review;if(canonicalDigest(reviewBody)!==reviewDigest||canonicalDigest(review)!==canonicalDigest(expected)||approvedReviewDigest!==reviewDigest)throw new Error("Missing or invalid explicit Lending Stage-1 approval");
  const transactions=manifest.orderedActions.map((action,index)=>{const approved=review.deployments[index];return{order:index+1,contract:action.contract,chainId:97,from:review.deployer,to:null,nonce:approved.nonce,value:"0",data:action.data,gasLimit:approved.approvedGasLimit,predictedAddress:approved.predictedAddress,expectedOwner:approved.owner,expectedGuardian:approved.guardian,initCodeDigest:approved.initCodeDigest}}),body={schemaVersion:1,recordType:"LQC_LENDING_STAGE1_UNSIGNED_EXECUTION_PACKET",status:"APPROVED_PACKET_REQUIRES_FRESH_SIGNING_PREFLIGHT",network:manifest.network,manifestDigest:manifest.manifestDigest,preflightDigest:preflight.preflightDigest,approvedReviewDigest:reviewDigest,deployer:review.deployer,transactions,maximumGasBudgetWei:review.budget.maximumGasBudgetWei,signingRequirements:["recheck-chain-97","recheck-current-nonce-105","recheck-two-empty-predicted-addresses","recheck-current-gas-and-balance","sign-one-transaction-at-a-time","verify-first-receipt-before-second"],transactionOccurred:false,safety:"Unsigned packet only. No private key, wallet request, signature or transaction broadcast mechanism."};return{...body,packetDigest:canonicalDigest(body)};
}
async function main(){const[manifestFile,configFile,preflightFile,reviewFile,approvedDigest,outputFile]=process.argv.slice(2);if(!manifestFile||!configFile||!preflightFile||!reviewFile||!approvedDigest)throw new Error("Usage: node prepare-lending-stage1-execution-packet.mjs <manifest> <config> <preflight> <review> <approved-digest> [output]");const read=file=>JSON.parse(fs.readFileSync(path.resolve(file))),input=read(configFile),config=input.recordType==="LQC_LENDING_STAGE0_FINALIZED_CONFIG"?input.config:input,result=prepareLendingStage1ExecutionPacket({manifest:read(manifestFile),config,preflight:read(preflightFile),review:read(reviewFile),approvedReviewDigest:approvedDigest});if(outputFile)fs.writeFileSync(path.resolve(outputFile),`${JSON.stringify(result,null,2)}\n`,{flag:"wx"});else console.log(JSON.stringify(result,null,2))}
if(process.argv[1]&&import.meta.url===pathToFileURL(process.argv[1]).href)main();
