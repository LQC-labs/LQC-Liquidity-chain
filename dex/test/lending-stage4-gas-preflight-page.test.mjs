import assert from "node:assert/strict";import fs from "node:fs";const j=fs.readFileSync(new URL("../app/lending-stage4-gas-preflight-testnet.js",import.meta.url),"utf8");describe("LQC Stage4 gas observation page",()=>{it("pins nonce 110 and predicted Engine",()=>{assert.ok(j.includes("N=110"));assert.ok(j.includes("0xEb948704"))});it("uses two RPCs and reads nonce balance gas price",()=>{for(const x of["publicnode.com","bnbchain.org","eth_getTransactionCount","eth_getBalance","eth_gasPrice"])assert.ok(j.includes(x))});it("contains no wallet signing or broadcast",()=>{assert.ok(!j.includes("eth_sendTransaction"));assert.ok(!j.includes("eth_requestAccounts"));assert.ok(!j.includes("PRIVATE_KEY"))})});
