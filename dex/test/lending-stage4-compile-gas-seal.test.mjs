import assert from "node:assert/strict";import fs from "node:fs";const s=fs.readFileSync(new URL("../scripts/build-lending-stage4-compile-gas-seal.mjs",import.meta.url),"utf8"),o=JSON.parse(fs.readFileSync(new URL("../deployments/lending-stage4-fresh-gas-observation-bsc-testnet-97.json",import.meta.url),"utf8"));describe("LQC Stage4 compile gas seal gate",()=>{it("pins fresh nonce gas price and balance while leaving browser CREATE gas null",()=>{assert.equal(o.deployerNonce,"110");assert.deepEqual(o.gasPriceGwei,["0.1","0.1"]);assert.equal(o.createGasEstimate,null)});it("requires compile-backed initCode estimate on two RPCs and 20 percent gas margin",()=>{for(const x of["buildLendingStage4Manifest","estimateGas","providers.length<2","*120n/100n","engineInitCodeDigest","maximumGasBudgetTbnb"])assert.ok(s.includes(x),x)});it("remains read-only",()=>{assert.ok(!s.includes("sendTransaction"));assert.ok(!s.includes("PRIVATE_KEY"))})});
