import assert from "node:assert/strict";import fs from "node:fs";const s=fs.readFileSync(new URL("../scripts/build-lending-stage4-browser-gas-packet.mjs",import.meta.url),"utf8");describe("LQC Stage4 browser gas packet",()=>{it("exports exact compile-backed Engine initCode for read-only estimate",()=>{for(const x of["buildLendingStage4Manifest","engineInitCode:a.data","engineInitCodeDigest","READ_ONLY_GAS_ESTIMATE_ONLY"])assert.ok(s.includes(x),x)});it("pins nonce 110 predicted address and no execution path",()=>{assert.ok(s.includes('deployerNonce!=="110"'));assert.ok(s.includes("getCreateAddress"));assert.ok(!s.includes("sendTransaction"));assert.ok(!s.includes("PRIVATE_KEY"))})});
