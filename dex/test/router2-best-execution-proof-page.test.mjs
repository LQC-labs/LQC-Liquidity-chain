import assert from "node:assert/strict";import fs from "node:fs";describe("Router 2.0 Best Execution Proof page",function(){const js=fs.readFileSync(new URL("../app/router2-best-execution-proof-testnet.js",import.meta.url),"utf8"),html=fs.readFileSync(new URL("../app/router2-best-execution-proof-testnet.html",import.meta.url),"utf8");it("loads the reviewed SDK and creates no transaction",function(){assert.match(html,/router-sdk\.js/);assert.match(html,/거래 없음/);assert.doesNotMatch(js,/eth_sendTransaction/)});it("rejects missing or stale preflight evidence",function(){assert.match(js,/OFFCHAIN_PROOF_PREFLIGHT/);assert.match(js,/Date\.now\(\)-p\.at>300000/)});it("hashes both exact route encodings into one proof",function(){assert.match(js,/ethers\.solidityPacked/);assert.match(js,/ethers\.AbiCoder/);assert.match(js,/routeDataHash:ethers\.keccak256/);assert.match(js,/candidates:\[/)});it("builds and independently validates the proof before saving",function(){assert.match(js,/buildBestExecutionProof/);assert.match(js,/verifyBestExecutionProof/);assert.match(js,/validateExecutionPlanProof/);assert.match(js,/localStorage\.setItem\(KEY/)})});
