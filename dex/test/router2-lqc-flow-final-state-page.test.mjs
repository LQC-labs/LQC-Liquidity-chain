import assert from "node:assert/strict";import fs from "node:fs";describe("Router 2.0 LQC Flow final-state page",function(){const js=fs.readFileSync(new URL("../app/router2-lqc-flow-final-state-testnet.js",import.meta.url),"utf8"),html=fs.readFileSync(new URL("../app/router2-lqc-flow-final-state-testnet.html",import.meta.url),"utf8");it("is read-only and recovers the one stored transaction hash",function(){assert.match(html,/읽기 전용/);assert.match(js,/localStorage\.getItem\(KEY\)/);assert.doesNotMatch(js,/eth_sendTransaction/)});it("binds receipt, signer, router, method and LQC Flow event",function(){for(const x of[/eth_getTransactionByHash/,/eth_getTransactionReceipt/,/0x5d4d7658/,/SwapExecuted 이벤트/,/topics\[2\]/])assert.match(js,x)});it("requires 10 tLQC, positive WBNB output and zero remaining allowance",function(){assert.match(js,/AMOUNT = 10n \* 10n \*\* 18n/);assert.match(js,/BigInt\(`0x\$\{data\[3\]\}`\) <= 0n/);assert.match(js,/BigInt\(allowance\) !== 0n/);assert.match(html,/전체 거래 해시 복사/)})});
