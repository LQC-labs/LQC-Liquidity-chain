import assert from "node:assert/strict";import fs from "node:fs";const s=fs.readFileSync(new URL("../scripts/build-lending-stage4-execution-approval.mjs",import.meta.url),"utf8");describe("LQC Stage4 execution approval gate",()=>{it("requires matching final review and compile gas seal",()=>{for(const x of["review.manifestDigest!==gasSeal.manifestDigest","review.deployerNonce!==gasSeal.deployerNonce","review.predictedLiquidationEngine","gasSeal.predictedLiquidationEngine"])assert.ok(s.includes(x),x)});it("locks four actions nonce 110 4-of-7 and engine gas budget",()=>{for(const x of["Exactly four ordered Stage4 actions only","nonce 110","4/7","maximumEngineGasBudgetTbnb"])assert.ok(s.includes(x),x)});it("is approval-only with no execution path",()=>{assert.ok(!s.includes("sendTransaction"));assert.ok(!s.includes("PRIVATE_KEY"));assert.ok(s.includes("transactionOccurred:false"))})});
