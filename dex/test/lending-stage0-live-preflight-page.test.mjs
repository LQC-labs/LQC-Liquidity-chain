import assert from "node:assert/strict";import fs from "node:fs";
describe("Lending Stage-0 live preflight page",function(){const html=fs.readFileSync(new URL("../app/lending-stage0-live-preflight-testnet.html",import.meta.url),"utf8"),js=fs.readFileSync(new URL("../app/lending-stage0-live-preflight-testnet.js",import.meta.url),"utf8");it("checks exact four-feed CREATE state and bounded gas across two RPCs",function(){for(const value of ["eth_chainId","eth_getBlockByNumber","eth_getTransactionCount","eth_getBalance","eth_getCode","eth_estimateGas"])assert.match(js,new RegExp(value));assert.match(js,/FEEDS=\[/);assert.match(js,/FEEDS\.map/);assert.match(js,/\*100n>high\*5n/);assert.match(js,/\*120n\/100n/)});it("is read-only and keeps the private endpoint ephemeral",function(){assert.match(html,/type="password"/);assert.match(js,/credentials:"omit"/);assert.match(js,/history\.replaceState/);assert.match(js,/pagehide/);assert.doesNotMatch(js,/localStorage|sessionStorage|window\.ethereum|eth_sendTransaction|eth_requestAccounts|privateKey|mnemonic/)});it("contains no embedded QuickNode credential",function(){assert.doesNotMatch(html+js,/quiknode\.pro\/[^\s"']+/i)})});
