import assert from "node:assert/strict";import fs from "node:fs";describe("Router 2.0 best-route preflight page",function(){const js=fs.readFileSync(new URL("../app/router2-best-route-preflight-testnet.js",import.meta.url),"utf8"),html=fs.readFileSync(new URL("../app/router2-best-route-preflight-testnet.html",import.meta.url),"utf8");it("is read-only and compares exactly two reviewed routes",function(){assert.match(html,/읽기 전용/);assert.match(js,/count!==2n/);assert.match(js,/ids\[0\]!==V3ID\|\|ids\[1\]!==FLOWID/);assert.doesNotMatch(js,/eth_sendTransaction/)});it("requires execution adapters, remaining capacity, and live quotes",function(){for(const x of[/0x721717c0/,/MAXDAY-used<AMOUNT/,/Promise\.all\(\[quote\(V3,v3path\),quote\(FLOW,flowpath\)/])assert.match(js,x)});it("stores the selected route with a 99 percent minimum and block",function(){assert.match(js,/out\*99n\/100n/);assert.match(js,/localStorage\.setItem\(KEY/);assert.match(html,/자동 선택 예상/)})});
